Coloca aquí los archivos de sonido para los fuegos artificiales.

Nombres sugeridos (y usados por defecto en `src/main.ts`):
- /assets/explosion0.mp3
- /assets/explosion1.mp3
- /assets/explosion2.mp3

Puedes cambiar las rutas en `src/main.ts` si prefieres otros nombres o carpetas.

Nota: los navegadores bloquean la reproducción automática de audio hasta que el usuario interactúe con la página. Para asegurarte de que el sonido suene durante la celebración automática, pide al usuario una interacción previa (por ejemplo, un clic en la página) o mantén la opción de sonido como opcional.
